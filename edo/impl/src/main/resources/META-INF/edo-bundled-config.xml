<?xml version="1.0" encoding="UTF-8"?>
<config>
    <!-- This holds configurations which are needed when running in bundled mode -->
    
    <!-- Rice module run modes -->
    <param name="kr.mode">LOCAL</param>
    <param name="core.mode">LOCAL</param>
    <param name="coreservice.mode">LOCAL</param>
    <param name="ksb.mode">LOCAL</param>
    <param name="kew.mode">LOCAL</param>
    <param name="kim.mode">LOCAL</param>
    
    <!-- In bundled mode all of the Rice URLs should point to the EDO app -->
    <param name="rice.app.code">${app.code}</param>
    <param name="rice.env.code">${env.code}</param>
</config>

